<Page x:Class="EmpyrionManagementSuite.Views.Startup"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:EmpyrionManagementSuite.Views"
      DataContext="{Binding Source={StaticResource Locator}, Path=Startup}"
      mc:Ignorable="d"
      d:DesignHeight="530"
      d:DesignWidth="800"
      Title="Startup">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Style="{StaticResource HeaderOne}" Text="{Binding Name}" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>